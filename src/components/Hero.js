import React from "react";

export default function Hero() {
    return (
        <section className="hero">
            <img src="./images/hero-gallery.png" alt="hero gallery" className="hero-banner" />
            <h1 className="hero-title">Online Experiences</h1>
            <p className="hero-subtitle">Join unique interactive activities led by one-of-a-kind hosts—all without leaving home.
            </p>
        </section>
    );
}